name: Busted
on: [push, pull_request]
jobs:
  sile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: CD to ./lua folder
        run: |
          cd lua
      - name: Unit Tests
        uses: lunarmodules/busted@v2.2.0
